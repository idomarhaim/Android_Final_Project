# session-identity-tabs — 2026-08-15

> **Summary:** Derived per 🧭 *shared singletons are exclusive* and *"if two claims would overlap, it is one session's work"*, rather than asked.

`/wayfinder 12` in `AUTO MODE`. **No ticket claimed, and that was the right outcome** — plus one
KB candidate filed for an error this session made and corrected.

## What happened

**Map [#12](https://github.com/idomarhaim/Android_Final_Project/issues/12)'s frontier was empty
and its body was held.** On opening (14:04) the map had 28 sub-issues, **28 of 28 closed**, so
*Work through the map* step 2 had nothing to take. The one remaining move — graduate a fog bullet
into a ticket — was already claimed by `c21-offline-story`, whose Active row declared `#12`'s body
as a singleton and whose transcript's last turn was **60 seconds old**. Stood down before the
first write: no claim, no board row, no edit to `#12`.

Derived per 🧭 *shared singletons are exclusive* and *"if two claims would overlap, it is one
session's work"*, rather than asked. **`AUTO MODE` was noted as not lifting it** — it governs the
commit / push / KB-ingest gates, not claim exclusivity.

`c21` went on to create, resolve and close
[`#43`](https://github.com/idomarhaim/Android_Final_Project/issues/43) (`d187649`, `5b1b6f9`) and
released at 14:11. Two consecutive `gh issue view 12` fetches were `cmp`-compared to confirm the
body was quiet before this session touched nothing.

## The error, and what it cost

**Reported to Ido that he had fired the same prompt into four sessions, three of them racing the
empty frontier, and recommended closing two.** Wrong. The §5.3(c) transcript check was run
correctly for **liveness** and then over-read for **identity**: `17b2f09b` and `6e0244ad` opened
with the byte-identical prompt on **2026-08-12**, three days earlier, and had moved to unrelated
work — `6e0244ad` was in `C:\Dev\JARVIS` on `rules/agent-topology-and-model-routing.md` with an
armed watch on `sibling-wait-banner`. **Acting on the recommendation would have killed a session
mid-unit.**

Found only because Ido asked *"which of the sessions in the image are doing the same task?"* and
the tab strip rendered the collision as visual triplets. Corrected in the same exchange.
`Observed:` all four transcripts' first and last turns, read directly.

## Tests

**No layer ran, and none was owed** — this session wrote no code and touched no
`app/`, `functions/` or `firestore-tests/` path. The two files added are documentation.

## KB candidates

📥 **Filed, not drained:** `kb-candidates/2026-08-15-session-identity-tabs.md` — *a transcript's
first message identifies the session, not its current task.* 🟢 `AUTO MODE`-eligible (destination
`kb/dev/claude-code-surfaces.md`, supersedes nothing), **held** because the write is cross-repo
into `C:\Dev\JARVIS` and this session had no other unit to carry it.

## Board

**No Active row taken** — one commit into a clean tree with an empty board, per the ceremony rule
(`c11b-output-formats`'s 2026-08-15 precedent). `SESSIONS.md` was read, never written, so the
shared-file hazard was not in play.

## State on close

`#12` **OPEN, 0 open children, 28 decisions, 3 fog bullets** — all three recorded as un-sharp or
un-owned. `docs/PRODUCT_v0.3.md` exists. The map is one decision away from closable, and that
decision is Ido's: graduate a fog bullet, or close it.
