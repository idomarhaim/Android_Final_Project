# Changes — 15/07/2026

> **Branch:** `feat/goalpilot-implementation`

## 📚 Documentation — `/jarvis-ize` governance pass
Brought the repo under full JARVIS governance. Gap inventory → parity sync →
approved scaffold upgrades → verification (all four items user-approved):

- **Parity sync** (`Sync-AgentAssets.ps1`): already in sync — no `.mcp.json`,
  hooks block, or repo-local skills to project; `-Check` re-run after the pass
  reports **PARITY OK**.
- **`.github/instructions/general.instructions.md`** re-synced **v8 → v10**
  (verbatim library copy): graduate-insights bullet now targets `/kb-ingest` +
  knowledge bundles + the memory-promotion rule; new 📌 KB-candidate flagging
  convention; new decision-ladder bullet; new tracker-hybrid rule (one-way
  TODO → GitHub Issue).
- **`AGENTS.md`**: inserted the `## 🤖 Model routing & agent topology` section
  from `AGENTS.md.template` v4 (routing by decision density, topology ladder,
  subagent gate with no read-only exemption, derivable-decisions rule,
  surfaces split) and bumped the SOURCE marker **v2 → v4**. All project-owned
  content untouched.
- **`TODO/TODO.md`**: merged to the v2 template skeleton — added the SOURCE
  marker, priority-order section, status legend, and conventions block; all
  existing MUST/OPTIONAL/FUTURE backlog content kept verbatim.
- **`knowledge/` bundle created** (OKF): `index.md` + `log.md` stubs, plus
  `deployment-conventions.md` copied verbatim from the library (v1) — the
  conditional bootstrap item applies because this project deploys Firebase
  rules + Cloud Functions. `/kb-lint` mechanical pass: **CLEAN** (after
  linking `log.md` from the index).
- **Superseded skills:** none present (no repo-local `.claude/skills/`).
- **Conditionals:** Python/notebooks/predictive-modeling N/A; `/impeccable
  init` N/A (Compose = native mobile, not a web frontend).
- **Template-library backlog (JARVIS-side, not this repo):** Kotlin/Android
  has no canonical instructions template — the repo's `android.instructions.md`
  is project-owned with nothing to sync against.

## 📁 New / Modified Files
- `.github/instructions/general.instructions.md` — re-synced to user-template v10.
- `AGENTS.md` — routing section inserted; marker bumped to v4.
- `TODO/TODO.md` — merged to v2 template skeleton.
- `knowledge/index.md` *(new)*, `knowledge/log.md` *(new)*,
  `knowledge/deployment-conventions.md` *(new, library copy v1)*.
- `CHANGELOG/2026-07-15.md` — this entry.

## 🧪 Tests
- None — documentation/governance change only; no code paths affected. The
  project's test layers (JVM unit, instrumented) are untouched; verification
  was `Sync-AgentAssets -Check` (PARITY OK) + `Check-KbLinks` (CLEAN).
