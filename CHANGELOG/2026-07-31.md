# Changes — 31/07/2026

> **Branch:** `feat/goalpilot-implementation`

## ✨ Summary
Worked through the full fix list from the project status review: two
time-critical external deprecations that would have broken the demo, the two
dead Cloud Functions now surfaced in the UI, and a set of correctness fixes in
the social/leaderboard layer. Instrumented tests were **executed for the first
time** in this project — which surfaced (and fixed) a test-library
incompatibility with modern emulators.

## ⏰ Time-critical deprecations
- **GROQ model shutdown.** `llama-3.1-8b-instant` was deprecated by GROQ on
  2026-06-17 and shuts down **2026-08-16** — 16 days out, i.e. right around the
  demo. Repinned `DEFAULT_MODEL` to GROQ's recommended replacement
  **`openai/gpt-oss-20b`** in `functions/src/index.ts` and `functions/.env.example`.
  This mattered because a retired model fails *silently*: the client falls back
  to local tips by design (spec §8), so the AI would have just looked bland
  rather than visibly broken.
- **Cloud Functions runtime.** `nodejs20` was deprecated 2026-04-30 and is
  decommissioned 2026-10-30. Moved `firebase.json` and `functions/package.json`
  to **nodejs22** (supported to 2027).
- **Firebase CLI** was not installed on this machine — installed
  `firebase-tools` 15.25.1 globally, so `firebase deploy` is now available.

## 🤖 Dead LLM code → shipped features
- **`classifyTask` had no UI**, so the spec §6 *Bonus* ("automatic classification
  of tasks into goals, including a suggestion to create a new goal") was not
  demonstrable. Added the **"Smart add a task"** card to the dashboard:
  free-text task → `classifyTask` → a confirmation dialog showing the chosen
  goal (or a proposed new one) and the estimated points → the user confirms
  before anything is written. A `suggestedGoalId` that matches no real goal is
  discarded rather than trusted (spec §8: LLM output can be inconsistent).
- **`scoreTask` was never called** from the client at all. Wired it to a ✨
  button on the add-task row in `GoalDetailScreen`, which fills the points field
  — spec §6 Core, "point scoring for tasks". Falls back to a deterministic local
  estimate (`5 + 3×words`, clamped 5..50).

## 🐛 Correctness fixes
- **`addFriend` accepted any string.** A mistyped code wrote a friend edge
  pointing at nobody, permanently invisible everywhere. It now rejects uids with
  no `publicProfiles` document and surfaces the repository's own error text
  instead of a generic "Could not add friend".
- **Friends leaderboard could drop friends.** It fetched the global top-100 by
  points and filtered client-side, so any friend outside that window vanished —
  exactly the friend a new user just added. "Friends" mode now fetches profiles
  **by document id** (`whereIn` on `documentId()`, chunked at Firestore's
  30-value cap). Ranking moved into a pure, unit-tested
  `List<LeaderboardEntry>.rankedByPoints()`.
- **Friend code was the raw 28-character Firebase uid** — copy-pasteable but not
  typeable in a live demo. Added `FriendCode`: a 6-character code over an
  alphabet that omits the ambiguous `I O 0 1`. Allocated on first sign-in
  (uniqueness-checked, back-filled for existing accounts), mirrored onto
  `publicProfiles` for lookup, shown large on the Profile tab, and normalised as
  the user types in the add-friend dialog.
- **Repository flows pinned the wrong account.** Every `observe*` read
  `auth.currentUser` once at Flow-construction time, so signing out and into a
  second account in the same process served the first user's data — precisely
  the spec §7 two-user demo. Added `FirebaseAuth.uidFlow()` and rebuilt the goal,
  task, progress and social flows on top of it with `flatMapLatest`.
- **Doc/code mismatch:** `FirestorePaths.FRIENDS` claimed friends live under
  `publicProfiles/{uid}/friends/`; the code has always used `users/{uid}/friends/`.

## 🧹 Cleanups
- Replaced 4 deprecated `Icons.Filled.*` with their `Icons.AutoMirrored.Filled.*`
  equivalents (`Login`, `DirectionsRun`, `TrendingUp`, `Assignment`).
  Build warnings: **13 → 9**.
- The remaining 9 are all the legacy Google Sign-In API in
  `data/auth/GoogleAuthClient.kt`. **Deliberately not fixed** — the Credential
  Manager migration is a `TODO_FUTURE` item, and per `TODO/TODO.md` conventions
  future-tier work needs explicit go-ahead. It also replaces the entire sign-in
  flow and cannot be verified until a real Firebase project exists to sign in
  against. See TODO/TODO.md.

## 🧪 Tests
Layers this project has: **domain (unit)**, **data (unit)**, **ViewModel (unit)**,
**UI/instrumented (Compose)**. All were run.

- **JVM unit:** `./gradlew :app:testDebugUnitTest` → **27 tests, 27 passed,
  0 failed, 0 errors** (was 15 — +12 new).
  - `FriendCodeTest` (6, new) — length/alphabet invariants, no ambiguous
    characters, seeded determinism, normalisation, validation.
  - `LeaderboardRankingTest` (4, new) — points ordering, deterministic tie-break,
    ranks relative to the filtered set, empty input.
  - `RecommendationRepositoryFallbackTest` (4, +2) — added `scoreTask` fallback
    value and its 5..50 clamping.
  - Unchanged: `LevelingTest` (4), `GoalTest` (4), `BuildSummaryUseCaseTest` (2),
    `SignInViewModelTest` (3).
- **UI / instrumented:** `./gradlew :app:connectedDebugAndroidTest` on a
  `Pixel_10_Pro_XL` AVD (**API 37 / Android 17**) → **3 tests, 3 passed,
  0 failed**. This layer had **never been executed** before today.
  - Added `GoalCategoryIconTest` (2) — every category maps to a distinct icon,
    and the `PROJECTS` category renders its AutoMirrored icon (guards the icon
    migration above).
  - Existing `EmptyStateUiTest` (1) passes.
  - **First run failed 3/3** with
    `NoSuchMethodException: android.hardware.input.InputManager.getInstance` from
    `Espresso.onIdle` — Espresso 3.6.1 reflects on a private API removed in
    Android 15+, so no test body ever ran. Fixed by bumping
    `androidx.test.ext:junit` 1.2.1 → **1.3.0** and
    `androidx.test.espresso:espresso-core` 3.6.1 → **3.7.0**.
- **Cloud Functions:** `npm run build` (tsc) → **compiles clean**.
- **Build:** `./gradlew :app:assembleDebug` → **BUILD SUCCESSFUL**.
- **Not run:** server integration / endpoint / database layers — the GROQ proxy
  and Firestore rules still need a live Firebase project, which does not exist
  yet (see `TODO/TODO.md` MUST). No layer was skipped silently.

## 📁 New / Modified Files
**New**
- `app/src/main/java/com/idomarhaim/goalpilot/domain/model/FriendCode.kt`
- `app/src/main/java/com/idomarhaim/goalpilot/data/auth/AuthExt.kt`
- `app/src/test/java/com/idomarhaim/goalpilot/domain/FriendCodeTest.kt`
- `app/src/test/java/com/idomarhaim/goalpilot/domain/LeaderboardRankingTest.kt`
- `app/src/androidTest/java/com/idomarhaim/goalpilot/ui/GoalCategoryIconTest.kt`
- `CHANGELOG/2026-07-31.md` — this entry.

**Modified**
- Backend/config: `functions/src/index.ts`, `functions/.env.example`,
  `functions/package.json`, `firebase.json`, `gradle/libs.versions.toml`.
- Domain: `domain/model/User.kt`, `domain/model/Social.kt`,
  `domain/repository/RecommendationRepository.kt`,
  `domain/repository/SocialRepository.kt`.
- Data: `data/auth/AuthRepositoryImpl.kt`, `data/firestore/SocialRepositoryImpl.kt`,
  `data/firestore/GoalRepositoryImpl.kt`, `data/firestore/TaskRepositoryImpl.kt`,
  `data/firestore/ProgressRepositoryImpl.kt`, `data/firestore/dto/Dtos.kt`,
  `data/firestore/dto/Mappers.kt`, `data/remote/RecommendationRepositoryImpl.kt`,
  `core/util/Constants.kt`.
- UI: `feature/dashboard/DashboardViewModel.kt`, `feature/dashboard/DashboardScreen.kt`,
  `feature/goals/GoalDetailViewModel.kt`, `feature/goals/GoalDetailScreen.kt`,
  `feature/social/SocialViewModel.kt`, `feature/social/SocialScreen.kt`,
  `feature/profile/ProfileScreen.kt`, `feature/auth/SignInScreen.kt`,
  `feature/challenges/ChallengesScreen.kt`, `ui/components/GoalCategoryIcon.kt`.
- Tests: `app/src/test/java/.../data/RecommendationRepositoryFallbackTest.kt`.
- Docs: `README.md`, `AGENTS.md`, `docs/SETUP.md`, `docs/ARCHITECTURE.md`,
  `TODO/TODO.md`, `TODO/TODO_OPTIONAL/Integrations.TODO.optional.md`,
  `CHANGELOG/CHANGELOG_README.md`.

## ⚠️ Follow-ups
- Credentials are still placeholders — the app has never run against a real
  Firebase backend. **Blaze plan is required** (Cloud Functions *and*, since
  2026-02-03, Cloud Storage). See `docs/SETUP.md`.
- Verify the pinned GROQ model is still current before the demo:
  <https://console.groq.com/docs/deprecations>.
- Legacy Google Sign-In → Credential Manager migration awaits go-ahead.
