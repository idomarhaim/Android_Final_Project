# Changes — 02/07/2026

> **Branch:** `feat/goalpilot-implementation`

## 📚 Documentation
- Defined a global **Commits & pushing** policy in the user rules
  (`my-rules.instructions.md`, user-level): CHANGELOG-first; commit message =
  copy-paste of the CHANGELOG entry (don't rewrite); an `AUTO MODE` marker at the
  start of a message → auto-commit, otherwise normal mode → ask before committing;
  never push without explicit OK and never to the default branch without confirmation.
- Aligned the `general.instructions.md` template with that policy: bumped
  user-template **v7 → v8** and re-synced the repo copy — replaced the old
  "always run `/finish-task`, don't hand-roll commits" bullet with the mode-aware
  commit flow plus an explicit push guard.

## 📁 New / Modified Files
- `.github/instructions/general.instructions.md` — re-synced to user-template v8.
- `CHANGELOG/2026-07-02.md`, `CHANGELOG/CHANGELOG_README.md` — this entry.
- (User-level, outside the repo: `my-rules.instructions.md`,
  `templates/.github/instructions/general.instructions.md`.)

## 🧪 Tests
- None — documentation/config change only; no code paths affected.
