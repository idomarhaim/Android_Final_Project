# KB candidates — `68-drag-to-move`, 2026-08-23

Each entry stands alone. No entry below depends on this session's transcript, per
`rules/memory-promotion.md`.

⚠️ **PARTLY DRAINED, TWICE, 2026-08-23 by session `68-drag-to-move`.** The file is rewritten down to
its one survivor and **not** deleted — original numbering kept, reasons below. What happened to the
other three:

- **№1** (the `\\`-in-a-double-quoted-string trap) — **INGESTED** into
  `C:\Dev\JARVIS\kb\dev\android-device-verification.md` as a second widening of §8a (JARVIS
  `d6f7dc1`). It is the **silent** third case of that section, and §8a's own prescribed fix is what
  leads you into it.
- **№2** (`showSnackbar` suspends, so an equal repeat is dropped) — **INGESTED as a SUPERSESSION**
  into `C:\Dev\JARVIS\kb\dev\screen-entry-effects-and-viewmodel-lifetime.md` §4a (JARVIS `74c021d`),
  **on Ido's authorisation**, given once he was told what it superseded. §4a's sentence *"A new
  event changes the key, restarting the effect"* is **qualified, not deleted** — it holds for a
  *different* second event — and the narrowing, the structural fix and two rejected alternatives go
  beneath it. The section heading was widened too: it now names two opposite failures on one
  primitive.
- **№3** (the `am instrument` variant suffix) — **DROPPED, already covered.** §8c of
  `android-device-verification.md` has said exactly this since 2026-08-20, including the
  `pm list instrumentation` habit. It was re-derived from a failed run because this session did not
  read the KB before its first device command, which this repo's `AGENTS.md` explicitly names as one
  of the three moments to do so. The drop is recorded in the journal rather than made silently,
  because *the KB's read path failing* is worth more than a duplicate section.

`Check-KbLinks` **CLEAN** (116 pages) after both ingests. Journal entries: `kb/log/2026-08-23.md`.

---

## Ready — additive, deferred only for thinness

## 4. Clamp before you snap, when the range's ends are not on the grain

**Claim.** Rounding a value to a grain and *then* clamping it into a range can leave the result off
the grain, because a range end need not be a multiple of the grain. Clamping first and snapping
afterwards (with one final coerce) cannot.

`Observed:` 2026-08-23, GoalPilot `#68`'s drag-landing arithmetic — the drawn grid ends at 23:00 and
the last legal block start is `23:00 − grain`, which is on the grain at 15 minutes and **off** it at
20. Caught by writing the test with a **20-minute** grain rather than the shipped 15, which is the
only reason the ordering was ever exercised.

**Why.** Thin as arithmetic, and recorded for the **test** lesson instead: a property that holds at
the shipped constant and fails at a neighbouring one is invisible to every test that uses the
shipped constant. The generalisable rule is *parameterise the constant in the test even when the
product has only one value for it*.

- **Destination:** `kb/dev/look-at-your-own-output.md`, merged into its *check the instrument on the
  hardest input it exists for* material — **not** a new section.
- **Anchors:** `kb/dev/look-at-your-own-output.md`
- **Supersedes:** nothing.
- **Status:** ready, **low value**. Deferred 2026-08-23 by `68-drag-to-move` because it wants
  merging into existing prose rather than appending, and that is better done by whoever next has
  that page open. **Nothing blocks it** — it is not always-ask and needs no decision.
