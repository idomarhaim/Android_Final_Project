# KB candidates — `c2-task-type`, 2026-08-13

Filed while resolving [#20 · `C2`](https://github.com/idomarhaim/Android_Final_Project/issues/20)
on map [#12](https://github.com/idomarhaim/Android_Final_Project/issues/12). Mode: `AUTO MODE`.
**Neither entry is drainable by this session** — see each Status. Each stands alone: no transcript
is a source, and nothing here may be reconstructed from one.

---

## 1 · ~~A write is a hypothesis until you have read it back~~ — **DRAINED 2026-08-13**

**Ingested** into `C:\Dev\JARVIS\kb\dev\runtime-verification.md` as **new §6**, *"Your own
effect on the system is a hypothesis too — and so is a read that came through an aggregate"*,
together with entry 1 of `kb-candidates/2026-08-13-c15b-stored-ai-text.md` — the same claim from the
opposite end. Index row rewritten; journalled in `kb/log/2026-08-13.md` with both source commits.

**Not drained separately, deliberately.** Each file proposed a section on the same page. A *write*
is a hypothesis until you read it back; an *aggregate read* is one too. Apart they read as two war
stories; together they state the boundary as **remote-state vs your belief about it**. A third case
from `c15b` was folded in and generalises both — a scan that ran in the wrong repo against an empty
range reported *clean*, because **a call reports its result and never its scope**.

**Drained by** session `c15b-stored-ai-text`, visiting `C:\Dev\JARVIS` with a row on that board,
on Ido's explicit *"do what you think is right, but verify it harms nothing"*. This entry belonged to `c2-task-type`, which had **released**, so it was an orphan in this folder rather than work in flight; draining it is recorded as the **agent's judgment**, not as an `AUTO MODE` entitlement.

**The full text of this entry is not reproduced here** — it is the committed §6, which is the
canonical form. This file is kept, not deleted, because entry 2 below is still parked.

---

## 2 · The fork check must run against the code, not against the ticket's own statement of the fork

**Claim.** `rules/question-axis-naming.md` requires grepping for a write path between two
quantities *"one hop wider than the two you named"* before drafting options. That rule is written
as if the two quantities come from **you**. When they come from a **committed artifact** — a
ticket, a spec, a brief — the widening has to start at the artifact's *premise*, not at its
*terms*, because the artifact states the fork as settled background rather than as a question, and
a premise stated as background is never re-derived. The tell is that the check **passes**: you
widen correctly around the two named quantities, find nothing, and never notice the fork's second
half was aimed at an object that cannot hold it.

**Why.** Observed 2026-08-13 on `#20 · C2`, whose title *is* the fork: *"is it a second axis or a
replacement for life areas?"* Four options were drafted, all varying along **which consumer would
read the new field**, and the fork-check was run over that axis and passed. Ido refused the picker
outright — *"I couldn't fully understand you or what each option means"* — and handed the decision
back. Deriving it instead took **three greps of `domain/model/`** and falsified the premise: the
app already ships `GoalCategory` (closed, ten, app-authored, model-assigned) beside `LifeArea`
(open, user-authored) as **two answers to one question on the same object**, with `Mappers.kt:29`
quietly resolving the conflict. So *replacement* had already happened, to a different field than
the one under discussion, and no option in the set could reach it.

Two things distinguish this from the failures already recorded in the rule:

1. **The axis was named and the naming did not help.** The question text explicitly named the axis
   (*consumers*) and listed the axes considered and dropped. Naming an axis tests whether your
   options span it; it cannot test whether the *question* is about a real distinction.
2. **The refusal was the only signal, and it was ambiguous.** Under the existing tell table,
   *"I couldn't understand the options"* routes to hand-back → ownership → premise → form →
   density. Premise is step 2 and would have caught it — but only if *"re-run the fork check"* is
   read as *re-run it against the world*, not *against the same two terms*. The proposed clause
   makes that explicit.

**Rejected alternative:** *"read the ticket more carefully"* — this ticket was read carefully; its
own **What it must settle** block is what supplied the four consumers, and it was written before
five of the decisions that killed three of them. Care does not fix a premise; a grep does.

**Destination.** `C:\Dev\JARVIS\rules\question-axis-naming.md` — a clause on **The widening**,
extending it from *derivation closures in code* to *premises in committed artifacts*.

**Anchors.** [`#20`'s resolution comment](https://github.com/idomarhaim/Android_Final_Project/issues/20#issuecomment-5273109754)
§0–§1 · `#12`'s `C2` decision line · this repo `b9d1be7` ·
[`Goal.kt#L59`](../app/src/main/java/com/idomarhaim/goalpilot/domain/model/Goal.kt),
[`Recommendation.kt#L36`](../app/src/main/java/com/idomarhaim/goalpilot/domain/model/Recommendation.kt),
[`Mappers.kt#L29`](../app/src/main/java/com/idomarhaim/goalpilot/data/firestore/dto/Mappers.kt).

**Supersedes.** Nothing outright — it **adds** to *The widening*. But it is adjacent to
`kb-candidates/2026-08-10-c9e-event-lifecycle.md` entry 2, which also proposes a second clause on
*The widening* (a closed sibling decision removing the premise). **The two should be merged into
one clause rather than shipped as two**, and the shape is close enough that shipping either alone
would make the other read as a duplicate.

**Status.** ⛔ **Always-ask, and doubly blocked — not drained, not proposed as a diff.**
1. Destination `rules/`, which `/kb-ingest` may not write and the 🎬 walkthrough gate owns.
2. **`rules/question-axis-naming.md` is held uncommitted by a live session right now** —
   `picker-queue-merge` claimed it on the JARVIS board at 00:37 and is merging **six** parked
   amendments into it and into `agent-topology-and-model-routing.md` §5 *as one reading*. This
   entry is a **seventh**, arriving mid-merge. It is deliberately **not** edited into that file
   and **not** raced onto that board: the right move is for it to join that reading, or the next
   one, as a whole — which is exactly the merge that session exists to do.
